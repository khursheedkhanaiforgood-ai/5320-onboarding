[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn server.app:app --host 0.0.0.0 --port $PORT"

[build.nixpacksPlan.phases.install]
cmds = ["/opt/venv/bin/pip install -r server/requirements.txt"]
